// Problem : U-Va ID: 514 - Rails

#include <cstdio>

int main () {
	// Input will consist of multiple lines
	
	// Case related variables
	int numberOfCoaches;

	while ( scanf("%d", &numberOfCoaches ) == 1 ){
		// if numberOfCoaches is equal to 0, the input has finished
		if ( numberOfCoaches == 0) return 0;
		
